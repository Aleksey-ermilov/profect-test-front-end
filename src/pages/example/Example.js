import React from 'react';

import { observer } from 'mobx-react'

import './example.scss'

const Example = observer(() => {

    return (
        <div>
            <p>example</p>
        </div>

    );
}) ;

export default Example;
